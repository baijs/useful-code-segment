{
  "name": "ftp-client",
  "version": "1.0.0",
  "description": "ftp client test",
  "main": "dist/index.js",
  "scripts": {
    "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
    "build": "tsc",
    "prestart": "npm run build",
    "start": "node .",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/ftp": "^0.3.29",
    "basic-ftp": "^3.4.4",
    "ftp": "^0.3.10"
  },
  "devDependencies": {
    "@types/node": "11.11.4",
    "tslint": "^5.14.0",
    "typescript": "3.3.4000"
  }
}
